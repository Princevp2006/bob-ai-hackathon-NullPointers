"""
PowerGuard AI — Database Initialisation Script

Usage:
    python src/scripts/init_db.py

Creates all SQLite tables from the SQLAlchemy models.
Run once before starting the application for the first time.
Also optionally seeds the database with sample data.

To be implemented in Phase 2.
"""

# TODO (Phase 2):
#   from src.backend.app import create_app
#   from src.backend.db.database import init_db, seed_db
#
#   app = create_app()
#   with app.app_context():
#       init_db(app)
#       seed_db()          # loads data/sample/ CSV files
#       print("Database initialised.")
